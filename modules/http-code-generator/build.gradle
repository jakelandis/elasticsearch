//TODO: fix the gradle build so it doesn't require a module == esPlugin
esplugin {
    description ''
    classname ''
}


dependencies {
    compile ('com.squareup:javapoet:1.11.1')
    compile project(path: ':modules:http-annotations')
}