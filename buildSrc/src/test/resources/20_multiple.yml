"Action to list contexts":
  replace:
    - value: bad_request
      with: something_else
    - value: aggregation_selector
      with: something_else2
    - object: { contexts.23: update}
      with: {c: d}
    - location: /foo/bar/match/0
      with: {c: d}
  add:
    - location: /foo/bar
      object: { contexts.22: update}
  remove:
    - location: /foo/bar
    - object: { contexts.22: update}
#---
#"Action to get all API values for score context":
#  - add:
#      - match.class_bindings.0.name: cosineSimilarity

